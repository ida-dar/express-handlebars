<!DOCTYPE html>
<html lang="en" dir="ltr">
  <h2>Contact</h2>
  <p>Send me an email at programmer@example.com</p>
  {{#if isSent}}
  <div class="isSent">
    <p>The message has been sent!</p>
    {{!-- <p>File {{ file }} has been saved</p> --}}
    <img class="file" src="/uploads/{{ file }}" alt="{{ file }}">
  </div>
  {{/if}}
  {{#if isError}}
  <div class="isError">
    <p>You can't leave fields empty!</p>
  </div>
  {{/if}}
  <form action="/contact/send-message" enctype="multipart/form-data" method="POST">
    <label>
      Your name: <input type="text" name="author">
    </label>
    <label>
      Your email: <input type="email" name="sender">
    </label>
    <label>
      Title: <input type="text" name="title">
    </label>
    <label>
      Project design (accepted files: .png, .jpg, .jpeg, .gif):
    </label>
    <input type="file" name="file" multiple="multiple" accept=".png,.jpg,.jpeg,.gif">
    <label>
      Message: <textarea name="message"></textarea>
    </label>
    <button type="submit">Send message</button>
  </form>
</html>